import 'package:flutter/material.dart';

class PrivacyScreen extends StatelessWidget {
  PrivacyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Privacy Page"),);
  }
}